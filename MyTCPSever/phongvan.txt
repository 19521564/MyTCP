TTH: đơn giản hóa cách sử dụng, tổng quát hóa cấu trúc của mình
Kế thừa: lớp con có thể kế thừa các thuộc tính phương thức protected public của lớp cha, và có thể bổ sung một số thuộc tính mới.
Đa hình: cùng 1 tên hàm nhưng khác nhau về tham số hoặc dữ liệu thì có thể có những phương thức riêng biệt
Có 3 loại constructor:
- mặc định.
- do mình tự định nghĩa (truyền tham số vào)
- copy từ một class khác.
Đóng gói: Che giấu cài đặt

Virtual: sử dụng hàm con chứ k phải hàm cha
bộ nhớ heap: con trỏ, k bị mất đi khi ctrinh kết thúc
bộ nhớ stack(1 MB): biến khai báo


STL:
vector là 1 mảng cấp phát động (thêm độ phức tạp O(1), xóa là O(n))
list là dslk các phần tử k liên tục với nhau trên vùng nhớ, thêm dô O(n),
set map là cây đỏ đen, mỗi phần tử k trùng lặp nhau (trùng lặp mutil set), truyền dô lấy ra là Log(n)


nhanh nhất là radix sort: không tổng quát, nhanh khi số lượng phần tử nhỏ
Quicksort là tổng quát nhất vì k phát sinh thêm vùng nhớ như heapsort 

Con trỏ hàm

tích chập và trôi, index